generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Prospecto {
  id_prospecto Int     @id @default(autoincrement())
  email        String  @unique
  senha        String
  nome         String
  documento    String
  status       Boolean
}
